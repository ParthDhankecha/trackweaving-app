name: trackweaving
description: "A new Flutter project.  name need to be change"

publish_to: 'none'

version: 1.2.4+24

environment:
  sdk: ^3.11.3

dependencies:
  flutter: { sdk: flutter }

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  country_code_picker: ^3.4.0
  shared_preferences: ^2.5.3
  json_annotation: ^4.11.0

  pinput: ^6.0.1
  google_fonts: ^8.0.0
  http: ^1.5.0
  percent_indicator: ^4.2.5
  blinker: ^1.0.2
  animated_toggle_switch: ^0.8.5
  intl: ^0.20.2
  flutter_svg: ^2.2.1
  package_info_plus: ^10.0.0

  #export packages
  path_provider: ^2.1.5
  pdf: ^3.11.0
  firebase_core: ^4.1.1
  firebase_messaging: ^16.1.3
  flutter_local_notifications: ^22.0.1
  url_launcher: ^6.3.2
  share_plus: ^13.2.0

dev_dependencies:
  build_runner: ^2.13.1
  flutter_lints: ^6.0.0
  json_serializable: ^6.13.1

flutter:
  uses-material-design: true
  assets:
     - assets/images/
     - assets/svgs/
